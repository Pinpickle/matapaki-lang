State { }

fun init: { } -> { }
  arg -> { }

export fun single_let_binding: { } -> Uint
  arg ->
    let x = 5;
    x + 1

export fun multiple_let_bindings: { } -> Uint
  arg ->
    let x = 5;
    let y = 6;
    let z = x + y;
    z + x + 5
