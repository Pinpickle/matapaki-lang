(jbuild_version 1)

(menhir
  ((modules (parser))))

(ocamllex
  (lexer))

(executable
 ((name main)
  (libraries (num core yojson cryptokit))
  (flags (:standard -P a))
  (ocamlopt_flags (:standard -p))))


